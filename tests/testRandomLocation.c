#include <carte.h>
#include <dobble-config.h>

#include <stdlib.h>
#include <time.h>
#include <stdio.h>

int main(int argc, char const *argv[])
{
	// srand(time(NULL));
    // Carte c = nouvelleCarte();
    // c.nbIcones = 1;
    // c.icones[0] = nouvelIcone();

    // // positionAleatoireIcones(&c);
    return 0;
}